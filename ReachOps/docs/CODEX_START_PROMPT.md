继续推进 ReachOps。

先读取仓库根目录 `AGENTS.md`，再读取：

1. `ReachOps/docs/REACHOPS_MASTER_EXECUTION_CONTRACT_V1.md`
2. `ReachOps/docs/REACHOPS_EXECUTION_STATE.md`

核对 Git、代码、测试和 PR 的真实状态。选择最高优先级且未被外部环境阻塞的 `READY` 工作，完成一个可审查、可回滚的 PR 级工程切片。不要重复询问已经写入合同的产品问题，不执行任何未授权真实 TikTok 动作，不把模拟、预检或文件存在写成业务成功。

完成后更新执行状态文件，并提交：验证事实、改动、测试、证据、阻塞、Commit SHA、回滚方式和下一动作。
